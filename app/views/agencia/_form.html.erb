<%= form_with(model: agencium, local: true) do |form| %>
  <% if agencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agencium.errors.count, "error") %> prohibited this agencium from being saved:</h2>

      <ul>
      <% agencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :agencia_id %>
    <%= form.text_field :agencia_id, id: :agencium_agencia_id %>
  </div>

  <div class="field">
    <%= form.label :numero_conta %>
    <%= form.text_field :numero_conta, id: :agencium_numero_conta %>
  </div>

  <div class="field">
    <%= form.label :limite %>
    <%= form.text_field :limite, id: :agencium_limite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
