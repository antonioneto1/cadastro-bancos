<%= form_with(model: agency, local: true) do |form| %>
  <% if agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agency.errors.count, "error") %> prohibited this agency from being saved:</h2>

      <ul>
      <% agency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label t('agency_nu') %>
    <%= form.text_field :agency_number, id: :agency_agency_number %>
  </div>

  <div class="field">
    <%= form.label t('address')  %>
    <%= form.text_area :endereco, id: :agency_endereco %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
